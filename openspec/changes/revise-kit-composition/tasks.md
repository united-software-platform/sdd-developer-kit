## 1. Дефекты файла поставки

- [ ] 1.1 Исправить в `docker-compose.yml` команду сервиса: `command: clauderegistry` → `command: claude`.
      Проверка: `docker compose --profile claude config` печатает `command: claude`, а `make up`
      оставляет контейнер запущенным (`docker compose ps` показывает состояние `running`).
- [ ] 1.2 Восстановить в `docker-compose.yml` оборванный комментарий о профиле (`не поднимаетL`).
      Проверка: строка читается как законченное предложение, посторонних символов в конце нет.
- [ ] 1.3 Заменить в комментарии `docker-compose.yml` ссылку `[READ-008]` формулировкой без кода
      правила. Проверка: `grep -R 'READ-008' .` вне каталогов `.git` и `openspec/changes` не находит
      совпадений.

## 2. Чистка правил чтения

- [ ] 2.1 Пересобрать перечень `READ-004` в `rules/reading-rules.md`: удалить `trash/`,
      `src/process/archive/`, `.claude/commands/on-check/`, внести `openspec/changes/archive/`.
      Проверка: каждый путь перечня существует в репозитории либо возникает в установленном проекте.
- [ ] 2.2 Удалить правило `READ-007` вместе с примечанием о `SREF-001`, ссылкой на
      `documentation-structure.md` и упоминаниями `STOR-001`..`STOR-018` — в таблице правил, тексте
      и разделе примера. Проверка: `grep -nE 'STOR-|SREF-' rules/` не находит совпадений вне таблицы
      версий.
- [ ] 2.3 Обобщить `READ-006`: убрать ссылки на `UC-001` и `SUB-DDC`, перевести формулировку
      на артефакты этого набора. Проверка: правило читается без обращения к внешним документам.
- [ ] 2.4 Убрать ссылку на `../constitution.md` (`M-006`) из обоснования запрета архива и ссылку
      на `git-rules.md` привести к действующему пути. Проверка: все относительные ссылки файла
      указывают на существующие файлы.
- [ ] 2.5 Обновить навигацию и таблицу правил `rules/reading-rules.md` под новый состав и дописать
      строку в таблицу версий по `DOC-022` и `DOC-024`. Проверка: пункты навигации соответствуют
      заголовкам, актуальная версия — последняя строка таблицы.

## 3. Чистка правил терминологии

- [ ] 3.1 Переформулировать `TERM-001` и `TERM-006` в `rules/terminology.md` без обязанности вести
      глоссарий; удалить ссылку на `../../src/process/glossary.md` и раздел «Единый источник истины»
      привести к новой формулировке. Проверка: правила выполнимы в наборе, где глоссария нет.
- [ ] 3.2 Удалить из `TERM-008` исключение для сабрутин с примерами `SUB-DDC` и `SUB-QLG`.
      Проверка: `grep -n 'SUB-' rules/terminology.md` не находит совпадений вне таблицы версий.
- [ ] 3.3 Обновить навигацию, таблицу правил и таблицу версий `rules/terminology.md`.
      Проверка: пункты навигации соответствуют заголовкам, новая запись — последняя строка таблицы.

## 4. Синхронизация точек входа

- [ ] 4.1 Добавить в `AGENTS.md` документ `rules/makefile-rules.md` в раздел «Правила кода»
      и дописать запись в таблицу версий. Проверка: перечень документов `rules/` в `AGENTS.md`
      и в `CLAUDE.md` совпадает поимённо.
- [ ] 4.2 Сверить остальные разделы `AGENTS.md` и `CLAUDE.md` на расхождения по составу правил
      и устранить найденные. Проверка: различие между файлами ограничено формой подключения
      (`@`-импорты против списка ссылок).

## 5. Версионируемый состав репозитория

- [ ] 5.1 Сверить `.claude/commands/opsx/` и `.claude/skills/openspec-*/` с выводом
      `make openspec-init` на текущем образе. Проверка: расхождений нет либо расхождения
      зафиксированы до удаления файлов из индекса.
- [ ] 5.2 Внести в `.gitignore` пути `.claude/commands/opsx/`, `.claude/skills/openspec-*/`
      и `.claude/settings.local.json`; снять эти файлы с версионирования, сохранив их в рабочем
      дереве. Проверка: `git status --short` пуст, `git ls-files .claude` ничего не выводит.
- [ ] 5.3 Дополнить раздел `README.md` «Разработка самого kit'а» указанием, что команды и skills
      агента в чистом клоне разворачивает `make openspec-init`. Проверка: раздел называет команду,
      и порядок работы в чистом клоне описан без пропущенного шага.

## 6. Проверка ссылочной целостности

- [ ] 6.1 Написать `tools/docs-lint/check.sh`: обход `README.md`, `CLAUDE.md`, `AGENTS.md`, `rules/`,
      `tools/**/README.md`; поиск относительных ссылок на несуществующие пути и кодов правил без
      определения в `rules/`; исключение раздела `## Версионирование` из проверки кодов.
      Проверка: коды возврата `0`/`1`/`2` соответствуют спецификации, вывод называет файл и строку.
- [ ] 6.2 Прогнать проверку на текущем состоянии репозитория и устранить находки либо обосновать
      исключение комментарием в сценарии. Проверка: `sh tools/docs-lint/check.sh` завершается кодом `0`.
- [ ] 6.3 Добавить `.github/workflows/docs-lint.yml` по образцу `makefile-lint.yml`: запуск
      на `pull_request` и `push` в `main`, вызов сценария без ключей. Проверка: прогон пайплайна
      на изменении даёт тот же вердикт, что локальный запуск.
- [ ] 6.4 Убедиться, что проверка не попадает в поставку: `tools/docs-lint` и файл пайплайна
      отсутствуют в `payload.txt`. Проверка: `sh install.sh --dry-run --source .` во временном
      каталоге не показывает их в плане.

## 7. Приёмка и версия

- [ ] 7.1 Прогнать существующие проверки репозитория: `sh tools/make-lint/check.sh`
      и `sh tools/version/check.sh`. Проверка: обе завершаются кодом `0` либо `tools/version/check.sh`
      сообщает об изменении состава поставки при неповышенной версии — ожидаемо до шага 7.3.
- [ ] 7.2 Выполнить установку изменённого набора в пустой каталог из локального источника и поднять
      окружение. Проверка: `make init` проходит, `make up` оставляет контейнер запущенным,
      `make shell` открывает оболочку.
- [ ] 7.3 Повысить версию репозитория релизным сценарием `release` с разрядом `minor` и кодом задачи,
      подтверждённым пользователем по `GIT-010`. Проверка: `VERSION` и аннотированный тег `vX.Y.Z`
      согласованы, `sh tools/version/check.sh` завершается кодом `0`.
