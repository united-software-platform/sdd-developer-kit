## 1. Удаление инструмента

- [x] 1.1 Удалить каталог `tools/docs-lint/` целиком. Проверка: `ls tools/` показывает только
      `host-runner` и `make-lint`; `git status --short` фиксирует удаление файла.
- [x] 1.2 Удалить `.github/workflows/docs-lint.yml`. Проверка: `ls .github/workflows/` содержит
      `docker-agent.yml`, `makefile-lint.yml` и `release.yml`.
- [x] 1.3 Проверить, что в действующих файлах репозитория не осталось упоминаний удалённой
      проверки. Проверка: `grep -rn 'docs-lint' . --include='*.md' --include='*.yml'` вне `.git`
      и `openspec/changes` не находит совпадений.

## 2. Спецификация

- [x] 2.1 Удалить из `openspec/specs/repo-rules/spec.md` требование «Ссылочная целостность
      документации проверяется автоматически» вместе с тремя его сценариями. Проверка: в спеке
      остаётся четыре требования, `openspec validate --specs --strict` проходит.
- [x] 2.2 Убедиться, что требование «Правила ссылаются только на существующие артефакты набора»
      не затронуто: норма остаётся действующей, удалён только её машинный контроль. Проверка:
      требование и его три сценария в спеке на месте.

## 3. Приёмка

- [x] 3.1 Прогнать `sh tools/make-lint/check.sh`. Проверка: код возврата `0` — вторая проверка
      репозитория не задета удалением первой.
- [x] 3.2 Убедиться, что состав поставки не изменился: `sh install.sh --dry-run --source .`
      во временном каталоге показывает те же 13 файлов, что и до изменения. Версия репозитория
      не повышается, релиз не требуется.
- [x] 3.3 Показать пользователю состав коммита и предложенное сообщение по `GIT-016`, запросить
      код и номер задачи по `GIT-010`. Проверка: коммит не создаётся до подтверждения.
