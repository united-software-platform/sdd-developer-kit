## 1. Разрешение выпуска базового образа и запись состава в метки

- [ ] 1.1 Перевести `FROM` в `Dockerfile` на аргумент сборки с выпуском базового образа и значением
  по умолчанию для локальной сборки; проверить, что `docker build` без аргументов собирается, а с
  переданным выпуском берёт указанный образ
- [x] 1.2 Разрешать выпуск и digest базового образа в задании параметров пайплайна: из полезной
  нагрузки события, если запуск пришёл от репозитория базового образа, иначе опросом реестра;
  проверить, что вывод задания называет разрешённый выпуск при каждом типе запуска
- [ ] 1.3 Передавать разрешённый выпуск в сборку и при проверке, и при публикации одним значением;
  проверить по логам обоих заданий, что значение совпадает
- [ ] 1.4 Проставлять метки выпуска и digest базового образа и метку выпуска утилиты OpenSpec;
  проверить чтением меток опубликованного образа, что состав читается без запуска контейнера
- [ ] 1.5 Перекрыть описательные метки образа (название, описание, адрес домашней страницы)
  значениями образа окружения агента; проверить, что в реестре образ не представляется именем
  базового образа
- [ ] 1.6 Сверять метки состава с разрешёнными версиями в задании проверки; проверить искусственным
  расхождением, что прогон останавливается до публикации и называет расхождение

## 2. Проверка совместимости утилиты OpenSpec

- [x] 2.1 Прогонять в задании проверки разбор и строгую валидацию артефактов репозитория утилитой
  из собранного образа; проверить, что на текущем состоянии репозитория проверка проходит
- [ ] 2.2 Убедиться, что неуспех проверки останавливает публикацию: прогнать задание с намеренно
  некорректным артефактом во временной ветке и увидеть, что образ в реестр не попал
- [ ] 2.3 Проверить, что сообщение неуспеха называет артефакт и выпуск утилиты — по выводу прогона
  из задачи 2.2

## 3. Отказ от пересборки при неизменном составе

- [x] 3.1 Читать метки состава опубликованного `latest` в задании параметров и сравнивать
  с разрешёнными версиями; проверить, что вывод задания печатает оба состава
- [ ] 3.2 Прекращать прогон до сборки, когда состав совпал, а исходник образа и пайплайн
  не изменялись; проверить повторным ручным запуском без внешних обновлений, что сборка
  не выполняется и прогон сообщает причину
- [ ] 3.3 Публиковать образ независимо от совпадения состава, когда запуск пришёл от релизного
  сценария; проверить, что тег версии появляется в реестре при релизе без внешних обновлений

## 4. Тег состава

- [ ] 4.1 Добавить в схему тегов публикации тег состава `X.Y.Z-YYYYMMDD-hhmmss` (UTC); проверить
  по составу манифеста, что тег проставлен вместе с тегом версии и `latest`
- [ ] 4.2 Проверить неперезаписываемость: выполнить две публикации без выпуска новой версии kit'а
  и убедиться, что в реестре доступны оба тега состава и оба адресуют разные образы
- [ ] 4.3 Проверить возврат к предыдущему составу: запустить контейнер с тегом состава предыдущей
  публикации и убедиться, что выпуск Claude Code в нём соответствует тому образу

## 5. Носитель разрешённых версий и бейджи

- [x] 5.1 Выбрать имя служебной ветки и путь JSON, создать ветку без исходников; проверить, что
  ветка не участвует в путях запуска пайплайнов и не попадает в состав поставки
- [ ] 5.2 Записывать разрешённые версии Claude Code и утилиты OpenSpec в JSON служебной ветки
  шагом после публикации образа; проверить, что файл доступен анонимным запросом и его значения
  совпадают с метками опубликованного образа
- [ ] 5.3 Перевести бейдж Claude Code и бейдж OpenSpec на этот носитель; проверить открытием
  `README.md`, что бейджи показывают выпуски из опубликованного образа, а не `latest` и не значение
  `ARG OPENSPEC_VERSION`
- [x] 5.4 Убедиться, что бейджи Python, uv, версии kit'а и лицензии не затронуты — сверить их
  значения с `Dockerfile`, `VERSION` и `LICENSE` после правки
- [x] 5.5 Дополнить `rules/git-rules.md` основанием для машинного коммита в служебную ветку —
  вторым именованным исключением либо оговоркой о границах запрета; проверить, что таблица правил,
  навигация и таблица версий документа приведены в соответствие
- [x] 5.6 Обновить в `CONTRIBUTING.md` раздел «Блок бейджей»: таблица «бейдж ↔ источник» и перечень
  ограничений; проверить, что каждый бейдж `README.md` имеет строку с фактическим источником

## 6. Событие от репозитория базового образа

- [ ] 6.1 Добавить в пайплайн образа приём события о публикации базового образа с выпуском
  и digest в полезной нагрузке; проверить ручной отправкой события, что прогон запускается
  и использует переданный выпуск
- [ ] 6.2 Добавить в пайплайн репозитория `united-software-platform/claude` отправку события после
  успешной публикации; проверить, что публикация в нём порождает прогон здесь
- [ ] 6.3 Выдать право на отправку события узко и проверить, что оно отзывается независимо
  от остальных прав репозитория
- [ ] 6.4 Убедиться, что расписание продолжает ловить обновления при неотправленном событии:
  прогнать плановый запуск и увидеть, что он подхватывает выпуск, о котором события не было

## 7. Форма ссылки на образ в поставке

- [x] 7.1 Проверить на актуальной версии Compose, поддерживается ли запись, задающая digest и тег
  разными переменными без переименования действующей переменной тега; зафиксировать результат
  проверки в `design.md`
- [ ] 7.2 Привести ссылку на образ в `docker-compose.yml` к выбранной форме; проверить запуск
  контейнера при заполнении файла окружения значениями по умолчанию, тегом версии, тегом состава
  и digest
- [ ] 7.3 Дополнить `.env.example` переменной адресации по digest с пустым значением
  по умолчанию; проверить, что установка в чистый проект даёт работоспособное окружение
  без правки файла окружения
- [x] 7.4 Если запись потребовала переименования действующей переменной, зафиксировать это
  в `proposal.md` как **BREAKING** и описать в `README.md` порядок правки `.env` установленных
  проектов

## 8. Документация и завершение

- [x] 8.1 Обновить раздел `README.md` «Образ окружения агента»: события пересборки, схема тегов,
  что означает тег версии и как закрепиться на составе; проверить, что номера версий в текст
  не вынесены и раздел ссылается на блок бейджей
- [x] 8.2 Обновить в `CONTRIBUTING.md` описание пайплайна образа: новый триггер, сверка состава,
  проверка совместимости, метки; проверить, что описание совпадает с фактическим поведением
  пайплайна
- [x] 8.3 Прогнать `openspec validate --all --strict` и убедиться, что артефакты набора корректны
  после правок документов
- [ ] 8.4 Выпустить версию kit'а: состав поставки изменён (`docker-compose.yml`, `.env.example`,
  `rules/git-rules.md`), разряд выбирает владелец репозитория при запуске релизного сценария
