## 1. Подготовка

- [x] 1.1 Зафиксировать эталон поведения: в чистом временном каталоге со снятой копией поставки
  выполнить текущий `make init`, сохранить список созданных файлов, вывод `ls -l .ssh`,
  полный текстовый вывод цели и вывод повторного `make init`. Служит образцом для сверки в 4.1
- [x] 1.2 Перечитать рецепт `init` в `Makefile` и разметить границы секций: `.env`, каталоги и
  права, `.gitignore`, проверка `ssh-keygen`, SSH-ключ, SSH-config, финальная подсказка

## 2. Цель `init-host`

- [x] 2.1 Добавить цель `init-host` с описанием `## Хостовая подготовка: .env, каталоги, SSH-ключ
  и конфигурация SSH, .gitignore`; перенести в её рецепт секции `.env`, `mkdir`/`chmod`, цикл по
  `.gitignore` из `init` без изменения команд. Проверка: `make init-host` в чистом каталоге создаёт
  `.env`, `.ssh` (права 700), `.claude-accounts`, записи в `.gitignore` — как в эталоне 1.1
- [x] 2.2 Перенести в рецепт `init-host` SSH-часть: в начало добавить блок shell-присвоений
  `ssh_key`/`ssh_config`/`git_host`/`git_user`/`container_ssh` с одним `$(call env_value,…)` и
  `:-`-умолчанием на переменную; проверку `command -v ssh-keygen` вынести отдельным `@`-шагом.
  Проверка: `make init-host` при отсутствующем `ssh-keygen` завершается тем же сообщением и кодом 1
- [x] 2.3 Разбить генерацию ключа и создание SSH-config на два отдельных `@`-блока внутри
  `init-host`, сохранив тексты сообщений, права (`chmod 600/644`) и вывод публичного ключа.
  Проверка: вывод `make init-host` для нового ключа и для случая пустого `GIT_HOST` совпадает
  с эталоном 1.1 построчно (кроме тела ключа)
- [x] 2.4 Добавить `init-host` в список `.PHONY`. Проверка: `make -n init-host` не зависит от
  наличия файла с именем `init-host`, цель всегда выполняется

## 3. Оркестратор `init` и `help`

- [x] 3.1 Свести рецепт `init` к последовательным `@$(MAKE) --no-print-directory init-host` и
  `@$(MAKE) --no-print-directory openspec-init` плюс существующий финальный `@echo`; уточнить
  описание `## Полная подготовка проекта: хостовая часть и инструменты SDD`. Проверка:
  `make init` в чистом каталоге даёт вывод и состав файлов, совпадающие с эталоном 1.1
- [x] 3.2 Проверить вывод `make help`: присутствуют `init`, `init-host`, `openspec-init` с
  описаниями, форматирование колонок не съехало
- [x] 3.3 Убедиться, что параллельный запуск не ломает записи в общий `.gitignore`: `make -j4 init`
  в чистом каталоге даёт корректный `.gitignore` без дублей и обрывов строк

## 4. Сверка и регрессия

- [x] 4.1 Полная сверка с эталоном 1.1: списки созданных файлов, права, тексты сообщений для
  `make init`, повторного `make init`, одиночного `make init-host` и `make init-host` в уже
  подготовленном проекте совпадают (кроме случайного содержимого ключа)
- [x] 4.2 Проверить идемпотентность отдельных под-целей: повторные `make init-host` и
  `make openspec-init` не перезаписывают `.env`, SSH-ключ и SSH-config и сообщают, что оставляют
  их как есть
- [x] 4.3 `openspec validate split-make-init --strict` проходит без ошибок
