## 1. Перечень путей поставки как данные

- [x] 1.1 Создать `payload.txt` в корне репозитория: по одному пути поставки на строку, тот же состав,
      что и в текущей переменной `PAYLOAD_PATHS`. Проверка: количество строк совпадает с количеством
      путей в переменной, пустых строк нет
- [x] 1.2 Перевести `install.sh` на чтение `payload.txt` вместо переменной `PAYLOAD_PATHS`; при
      отсутствии или пустоте файла — отказ с описанием причины. Проверка: `sh install.sh --dry-run
      --source .` во временном каталоге показывает тот же план, что и до перевода
- [x] 1.3 Убедиться, что `payload.txt` не входит в собственный список и не копируется в целевой проект.
      Проверка: после `--dry-run` установки `payload.txt` в плане отсутствует

## 2. Проверка пропущенной инкрементации

- [x] 2.1 Перевести `tools/version/check.sh` на чтение `payload.txt`; awk-разбор `PAYLOAD_PATHS`
      из `install.sh` удалить. Проверка: изменение `Dockerfile` без правки `VERSION` даёт ненулевой код
      и перечень изменённых путей
- [x] 2.2 Сохранить пропуск требования при изменениях вне состава поставки. Проверка: изменение только
      в `openspec/` завершается нулевым кодом
- [x] 2.3 Удалить из `check.sh` сверку тегов с содержимым `VERSION` вместе с ключом `--all-tags`:
      расхождение исключено по построению, релиз ставит тег тем же запуском. Проверка: `--help` больше
      не упоминает теги, прогон без ключей выполняет только проверку поставки

## 3. Релизный workflow

- [x] 3.1 Удалить `tools/version/bump.sh`. Проверка: файла нет, а `grep -rn "bump.sh"` не находит
      ссылок вне архива change'ей
- [x] 3.2 Создать `.github/workflows/release.yml` на `workflow_dispatch` с двумя входными параметрами:
      `bump` (выбор из `major`, `minor`, `patch`) и `task` (код и номер задачи). Проверка: файл
      разбирается как YAML, оба параметра обязательные, других событий запуска у workflow нет
- [x] 3.3 Реализовать шаги релиза: чтение и разбор `VERSION`, повышение разряда, запись, коммит
      с заголовком `<task>: релиз vX.Y.Z`, аннотированный тег `vX.Y.Z` и его публикация. Проверка:
      заголовок коммита удовлетворяет регулярному выражению из [GIT-009](../../../rules/git-rules.md#формат-сообщения-коммита)
- [x] 3.4 Добавить отказ при значении `VERSION`, не разбираемом как три числа, с выводом прочитанного
      значения; выдать workflow право `contents: write`. Проверка: шаг разбора версии виден в файле,
      права объявлены явно и ограничены этим сценарием

## 4. Проверочный workflow

- [x] 4.1 Оставить `.github/workflows/version-check.yml` на `pull_request` и `push` в `main`, вызывающим
      `tools/version/check.sh`. Проверка: файл разбирается как YAML без ошибок
- [x] 4.2 Сохранить базу сравнения по событию: точка ветвления от целевой ветки для `pull_request`,
      предыдущее состояние ветки для `push`; глубина `checkout` достаточна для доступа к базе.
      Проверка: в отправленной ветке шаг проверки находит базу и не падает на неполной истории

## 5. Бейдж и документация

- [x] 5.1 Изменить URL бейджа версии в `README.md` так, чтобы значение показывалось с префиксом `v`
      при неизменном источнике `VERSION`. Проверка: бейдж отображает `v1.0.0`, номер в тексте
      `README.md` не появляется
- [x] 5.2 Переписать подраздел «Версия репозитория» в `README.md` под новый механизм: носители версии,
      запуск релизного workflow с разрядом и задачей, локальный прогон проверки, `payload.txt` как
      единый перечень поставки. Проверка: раздел не упоминает `bump.sh` и называет оба параметра запуска
- [x] 5.3 Зафиксировать в `README.md` ограничения по [DOC-020](../../../rules/documentation-rules.md#полнота-содержания):
      проверка срабатывает после отправки изменений, разряд и задачу задаёт человек, механизм не входит
      в поставку и не встраивается в сценарии OpenSpec. Проверка: все три ограничения присутствуют в тексте
- [x] 5.4 Обновить строку в таблице версий `README.md` по [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа)
      и [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа) под итоговый состав
      правок. Проверка: новая строка — последняя в таблице, колонки задачи, агента и модели заполнены

## 6. Сквозная проверка

- [x] 6.1 Убедиться, что ни `tools/version`, ни `payload.txt`, ни файлы workflow не попадают в установку.
      Проверка: `sh install.sh --dry-run --source .` во временном каталоге не показывает ни одного
      из этих путей и ни файла `VERSION`
- [x] 6.2 Убедиться, что состав `make help` не изменился и все перечисленные цели выполнимы в
      установленном проекте. Проверка: вывод `make help` совпадает с текущим списком целей
- [x] 6.3 Подготовить исходный тег `v1.0.0` и предъявить пользователю для подтверждения; создание
      и push — по его явной команде. Проверка: после создания тег указывает на коммит, где `VERSION`
      содержит `1.0.0`
