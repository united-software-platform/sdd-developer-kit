## 1. Удаление целей управления контейнером

- [x] 1.1 Удалить из `Makefile` секции целей `up`, `down` и `shell` вместе с их комментариями;
      проверка — `make help` перечисляет только `init`, под-цели подготовки, `openspec-init`
      и `help`, а `make up` завершается сообщением `make` об отсутствующей цели
- [x] 1.2 Убрать `up`, `down`, `shell` из списка `.PHONY` в начале `Makefile`; проверка —
      `grep -n '\.PHONY' -A 3 Makefile` не содержит удалённых имён
- [x] 1.3 Прогнать `sh tools/make-lint/check.sh` по [MAKE-004](../../../rules/makefile-rules.md#локальный-прогон-проверки-make-004);
      проверка — код возврата `0`
- [x] 1.4 Переписать в `docker-compose.yml` комментарий монтирования профиля: ссылка на проверку
      «в цели make claude-shell» заменяется указанием, что профиль задаётся переменной окружения
      на запуске сессии; проверка — в файле нет упоминаний удалённых целей
      (`grep -n 'claude-shell\|make up\|make down' docker-compose.yml` пуст)

## 2. Белый список host-runner

- [x] 2.1 Привести `ALLOWED_TARGETS` в `tools/host-runner/runner.py` к составу
      `{"init", "openspec-init", "help"}`; проверка — запуск раннера печатает перечень из трёх целей
- [x] 2.2 Обновить `tools/host-runner/README.md`: раздел «Разрешённые цели» перечисляет три цели,
      пример отклоняемой цели `shell` заменяется на `up`/`down` с обоснованием (целей управления
      контейнером в `Makefile` нет); проверка — перечень в документе совпадает с `ALLOWED_TARGETS`
- [x] 2.3 Проверить вручную: запрос `GET /run/up` с корректным токеном отклоняется кодом `400`
      и перечисляет три разрешённые цели, запрос `GET /run/help` выполняется

## 3. Документ для контрибьюторов

- [x] 3.1 Создать `CONTRIBUTING.md` по [DOC-014](../../../rules/documentation-rules.md#структура-файла):
      заголовок, назначение, навигация; проверка — файл открывается тремя обязательными элементами
- [x] 3.2 Перенести в `CONTRIBUTING.md` содержимое разделов `README.md` «Разработка самого kit'а»
      и «Версия репозитория» и описание пайплайна сборки образа (события пересборки, порядок
      проверки перед публикацией, запуск сборки релизным сценарием, метки образа) по
      [DOC-023](../../../rules/documentation-rules.md#жизненный-цикл-документа); проверка —
      ключевые факты и ссылки на правила сохранены, относительные ссылки ведут в существующие файлы
- [x] 3.3 Добавить в `CONTRIBUTING.md` описание состава блока бейджей и источников их значений
      с ограничениями по [DOC-020](../../../rules/documentation-rules.md#полнота-содержания);
      проверка — по документу видно, какие бейджи затрагивает правка `ARG` в `Dockerfile`
- [x] 3.4 Завершить `CONTRIBUTING.md` таблицей версий по
      [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа); проверка —
      первая запись `1.0.0` с задачей `OpenSpec change revise-readme-and-startup`
- [x] 3.5 Убедиться, что `CONTRIBUTING.md` не добавлен в `PAYLOAD_PATHS` в `install.sh`;
      проверка — `sh install.sh --dry-run --source .` в тестовом каталоге не показывает файл в плане

## 4. README: порядок установки и запуск агента

- [x] 4.1 Удалить из «Порядка установки» шаг 6 «Войти в контейнер»; проверка — в разделе нет
      упоминаний `make shell`
- [x] 4.2 Заменить шаг 5 «Запустить контейнер» шагом запуска агента: команда
      `CLAUDE_PROFILE=<профиль> docker compose run --rm claude`, ожидаемый результат — работающая
      сессия агента; проверка — порядок заканчивается запуском агента, шагов после него нет
- [x] 4.3 Добавить раздел «Работа с агентом» с командой запуска, указанием профиля аккаунта,
      признаком неверного `CLAUDE_PROFILE` (агент просит авторизацию заново), командой обновления
      образа `docker compose pull claude` и ссылкой на репозиторий базового образа
      `united-software-platform/claude`; проверка — раздел не воспроизводит инструкцию источника,
      а ссылается на неё
- [x] 4.4 Снять из раздела «Команды» строки `make up`, `make shell`, `make down` и утверждение
      «прямые вызовы `docker compose` не нужны ни на одном шаге»; проверка — таблица команд
      совпадает с выводом `make help`
- [x] 4.5 Дополнить раздел «Повторная установка и обновление» ограничением: после обновления kit'а
      целей управления контейнером в проекте нет, агент запускается командой среды контейнеризации;
      проверка — ограничение сформулировано явно по
      [DOC-020](../../../rules/documentation-rules.md#полнота-содержания)

## 5. README: переменные окружения и границы документа

- [x] 5.1 Привести таблицу переменных окружения к полному составу `.env.example`, добавив
      `AGENT_RUNNER_PORT`, `AGENT_RUNNER_URL` и `AGENT_RUNNER_TOKEN`; проверка — каждое имя из
      `.env.example` находится в таблице (`grep '^[A-Z_]*=' .env.example` построчно)
- [x] 5.2 Добавить в таблицу признак обязательности каждой переменной (обязательная, по
      необходимости, значение по умолчанию из шаблона) и пометку для `CLAUDE_PROFILE` о том, что
      в шаблон она не входит; проверка — у каждой строки таблицы признак заполнен
- [x] 5.3 Удалить из `README.md` разделы «Разработка самого kit'а» и «Версия репозитория», оставив
      ссылку на `CONTRIBUTING.md`; проверка — `grep -n '^## ' README.md` не содержит удалённых
      заголовков
- [x] 5.4 Сжать раздел «Образ окружения агента» до пользовательских фактов: происхождение образа,
      закрепление версии `SDD_KIT_IMAGE_TAG` и подмена `SDD_KIT_IMAGE`, поведение без сети,
      неизменность тега при плановой пересборке; проверка — в разделе нет описания событий
      пересборки, пайплайна и порядка проверки образа перед публикацией
- [x] 5.5 Удалить из `README.md` раздел «Версионирование» вместе с таблицей версий; проверка —
      `grep -n '^## Версионирование' README.md` пуст, документ заканчивается разделом «Лицензия»
- [x] 5.6 Поправить ссылку раздела «Состав набора» на `CONTRIBUTING.md` вместо удалённого раздела
      «Разработка самого kit'а»; проверка — относительная ссылка ведёт в существующий файл
- [x] 5.7 Синхронизировать навигацию `README.md` с итоговым составом разделов; проверка — каждый
      пункт навигации ведёт в существующий раздел, а каждый раздел уровня `##` присутствует
      в навигации

## 6. Правила документации

- [x] 6.1 Переформулировать исключение [DOC-015](../../../rules/documentation-rules.md#ссылки-doc-015)
      как критерий «в `README.md` абсолютный URL допускается там, где цель ссылки действительно
      внешняя», оставив действующие места (блок бейджей, названия пунктов перечня состава)
      примерами и добавив ссылку на репозиторий базового образа; требование относительных ссылок
      на файлы репозитория сохраняется; проверка — строка `DOC-015` в таблице правил приведена
      к новой формулировке, перечня мест в правиле нет
- [x] 6.2 Добавить в [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа)
      исключение по типу документа: `README.md` таблицу версий не ведёт, история правок остаётся
      в системе контроля версий; прочие документы правило сохраняют. Строку `DOC-022` в таблице
      правил привести к новой формулировке; проверка — исключение названо явно и ограничено
      `README.md`
- [x] 6.3 Удалить таблицу версий из `tools/host-runner/README.md` по новому исключению `DOC-022`;
      проверка — `grep -rn '^## Версионирование' README.md tools/host-runner/README.md` пуст
- [x] 6.4 Дописать запись в таблицу версий `rules/documentation-rules.md` по
      [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа); проверка — новая
      строка последняя, версия повышена на разряд исправления

## 7. Завершение

- [x] 7.1 Проверить, что ни один документ, потерявший таблицу версий, не ссылается на неё из других
      файлов; проверка — `grep -rn 'README.md#версионирование' --include='*.md' .` пуст
- [x] 7.2 Пройти порядок установки в чистом тестовом каталоге до запуска агента: `install.sh
      --source .`, `make init`, заполнение `.env`, повторный `make init`,
      `CLAUDE_PROFILE=<профиль> docker compose run --rm claude`; проверка — каждый шаг выполняется
      командой, названной в `README.md`, и агент стартует
- [x] 7.3 Прогнать `openspec validate revise-readme-and-startup --strict`; проверка — вердикт
      `is valid`
- [x] 7.4 Показать пользователю состав коммита и предложенное сообщение по
      [GIT-016](../../../rules/git-rules.md#коммиты-и-push) и запросить код и номер задачи
      по [GIT-010](../../../rules/git-rules.md#формат-сообщения-коммита); проверка — коммит
      не создаётся до подтверждения
- [x] 7.5 Сообщить пользователю, что состав поставки изменился несовместимо и требуется релиз
      разрядом `major` отдельным запуском сценария `release`; проверка — решение о выпуске принимает
      пользователь, изменение его не выполняет
