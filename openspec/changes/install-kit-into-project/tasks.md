## 1. Интерфейс операций окружения

- [x] 1.1 Добавить в `Makefile` цель `init`: создаёт `.env` из `.env.example` только при его
      отсутствии, создаёт каталоги `.ssh/` и `.claude-accounts/`, дописывает их и `.env` в
      `.gitignore` проекта при отсутствии соответствующих строк — проверить двумя запусками подряд:
      второй не меняет ни `.env`, ни `.gitignore`
- [x] 1.2 Добавить в `Makefile` цель `shell`: вход в контейнер агента с проверкой заданного
      `CLAUDE_PROFILE` — проверить, что без `CLAUDE_PROFILE` цель завершается понятной ошибкой,
      а не входит в контейнер без профиля
- [ ] 1.3 Сверить, что порядок установки от команды установки до входа в контейнер проходится
      только целями `make` — проверить, что ни один шаг не требует прямого вызова `docker compose`

## 2. Установщик: получение состава поставки

- [x] 2.1 Перенести установщик в корень репозитория как `install.sh`, удалить `tools/install/` —
      проверить, что скрипт запускается по короткому пути и в репозитории не осталось второй копии
- [x] 2.2 Реализовать выбор загрузчика (`curl`, при отсутствии — `wget`) с понятной ошибкой, если
      нет ни одного — проверить обоими загрузчиками и с недоступными обоими
- [x] 2.3 Реализовать скачивание архива репозитория по `--ref` (по умолчанию `main`) во временный
      каталог с удалением каталога при любом завершении — проверить успешную загрузку и прерванную
      (недоступный хост, несуществующий ref): во втором случае временный каталог не остаётся
- [x] 2.4 Реализовать разбор аргументов (`--dry-run`, `--yes`, `--ref`, `--help`), доступный при
      запуске через конвейер (`sh -s -- …`) — проверить каждый ключ в форме `sh -s --`
- [x] 2.5 Реализовать работу в текущем каталоге без аргумента пути и проверку его доступности на
      запись до любых изменений — проверить запуск в каталоге без права записи: ошибка, ни одного
      файла не создано

## 3. Установщик: план, подтверждение, запись

- [x] 3.1 Задать явный список путей поставки (`Dockerfile`, `docker-compose.yml`, `Makefile`,
      `.env.example`, `rules/`, `CLAUDE.md`, `AGENTS.md`, `.claude/skills/`, `.claude/commands/`,
      `openspec/config.yaml`, `tools/host-runner/`, `SDD-KIT.md`) и прерывать установку ошибкой,
      если путь отсутствует в распакованном архиве — проверить удалением пути из архива
- [x] 3.2 Реализовать построение плана по контрольным суммам манифеста (создание, безопасное
      обновление, конфликт) без записи — проверить сверкой содержимого каталога до и после
- [x] 3.3 Реализовать режим предварительного просмотра: вывести план и завершиться без записи —
      проверить, что каталог не изменился
- [x] 3.4 Реализовать запрос подтверждения через `/dev/tty` с выводом перечня перезаписываемых
      файлов — проверить запуском через конвейер `curl … | sh` (или его эквивалентом с локальным
      файлом) в терминале: вопрос задаётся, несмотря на занятый стандартный ввод
- [x] 3.5 Реализовать отказ и отсутствие терминала: при ответе «нет» и при отсутствии `/dev/tty`
      без `--yes` завершаться без изменений — проверить оба сценария сверкой содержимого каталога
- [x] 3.6 Реализовать запись файлов по плану и манифест `.sdd-kit-manifest.json` последним шагом —
      проверить, что перечень в манифесте совпадает с фактически записанными файлами, а версия
      взята из `VERSION` архива
- [x] 3.7 Убедиться, что в целевой проект не попадают `openspec/changes/`, `openspec/specs/`,
      сам установщик, `README.md`, `.env`, каталоги профилей и SSH-ключей — проверить их отсутствие
      после установки в пустой каталог

## 4. Документация

- [x] 4.1 Создать `SDD-KIT.md`: нумерованный порядок установки (команда установки → `make init` →
      заполнение `.env` и SSH-ключ → `make build` → `make up` → `make shell`), таблица целей `make`,
      переменные окружения, ограничения — проверить, что каждый шаг называет команду и ожидаемый
      результат
- [x] 4.2 Переписать `README.md` как витрину: назначение kit'а, одна команда установки, вариант с
      предварительным просмотром скрипта, ссылка на `SDD-KIT.md` — проверить, что порядок шагов
      описан только в `SDD-KIT.md` и не дублируется
- [x] 4.3 Удалить `docs/sdd-kit.md` и `tools/install/README.md`, перевести ссылки в `CLAUDE.md`
      и `AGENTS.md` на `./SDD-KIT.md` — проверить, что в репозитории не осталось ссылок на удалённые
      файлы
- [x] 4.4 Добавить `SDD-KIT.md` в состав поставки установщика — проверить, что после установки
      инструкция лежит в корне целевого проекта

## 5. Проверка

- [ ] 5.1 Проверить установку в чистый каталог удалённой командой из опубликованной ветки —
      установка проходит, состав файлов совпадает со списком поставки, манифест корректен
- [ ] 5.2 Пройти порядок установки из `SDD-KIT.md` целиком на чистом проекте: установка →
      `make init` → заполнение `.env` → `make build` → `make up` → `make shell` — каждый шаг
      завершается ожидаемым результатом
- [x] 5.3 Сверить каждое требование `specs/kit-installation/spec.md` (установка без клонирования,
      состав поставки, структура, цели `make`, инструкция с порядком, обнаружение конфликтов,
      подтверждение через терминал, отказ, предварительный просмотр, неинтерактивный режим, манифест,
      проверка каталога) с поведением реализации — каждое требование должно иметь воспроизводимую
      проверку
- [x] 5.4 Выполнить `openspec validate install-kit-into-project --strict` и убедиться, что команда
      завершается без ошибок
