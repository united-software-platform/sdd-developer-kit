# readme-usage Specification

## Purpose

Описывает `README.md` репозитория kit'а как инструкцию пользователя набора: что читатель обязан
найти в документе, чтобы развернуть окружение и запустить агента, и какой материал в документ
не входит, потому что адресован разработчику самого набора.

## Requirements

### Requirement: README — инструкция пользователя набора
`README.md` SHALL описывать применение набора: установку в проект, подготовку проекта, запуск агента
и назначение переменных окружения. Материал, адресованный разработчику самого набора — устройство
пайплайнов сборки и публикации образа, локальные проверки репозитория, механизм версионирования
и релиза, состав и источники значений блока бейджей, — MUST NOT размещаться в `README.md`.

Такой материал SHALL размещаться в отдельном документе репозитория, не входящем в состав поставки.
`README.md` SHALL содержать ссылку на этот документ по
[DOC-023](../../../rules/documentation-rules.md#жизненный-цикл-документа).

Раздел `README.md` об образе окружения агента SHALL ограничиваться фактами, наблюдаемыми
пользователем: откуда берётся образ, как закрепить его версию или подставить свою сборку и что
происходит при недоступности реестра. События пересборки, состав пайплайна и порядок проверки образа
перед публикацией MUST NOT входить в этот раздел.

#### Scenario: Пользователь разворачивает набор
- **WHEN** пользователь читает `README.md`, чтобы развернуть набор в своём проекте
- **THEN** он находит установку, подготовку проекта, запуск агента и переменные окружения, и ни один
  раздел документа не описывает устройство пайплайнов или релиза kit'а

#### Scenario: Контрибьютор ищет порядок релиза
- **WHEN** разработчик самого набора ищет, как выпускается новая версия и как запускаются проверки
  репозитория
- **THEN** он попадает в документ для контрибьюторов по ссылке из `README.md`, а сам порядок
  в `README.md` не продублирован

#### Scenario: Документ контрибьютора при установке в проект
- **WHEN** kit устанавливается в целевой проект установочным скриптом
- **THEN** документ для контрибьюторов в целевой проект не попадает

### Requirement: Раздел запуска агента в контейнере
`README.md` SHALL содержать раздел о работе с Claude Code в контейнере. Раздел SHALL называть команду
запуска агента, способ указания профиля аккаунта и порядок обновления образа окружения до актуального.

Раздел SHALL ссылаться на репозиторий базового образа как на источник подробностей и MUST NOT
воспроизводить его инструкцию целиком: в `README.md` остаётся запуск агента в этом наборе, а полное
описание работы с образом ведётся в одном месте — репозитории образа.

Ссылка на репозиторий базового образа SHALL быть абсолютным URL: цель ссылки внешняя и относительной
ссылкой не выражается. Исключение SHALL быть зафиксировано в
[DOC-015](../../../rules/documentation-rules.md#ссылки-doc-015).

#### Scenario: Пользователь запускает агента впервые
- **WHEN** пользователь прошёл подготовку проекта и открывает `README.md`, чтобы начать работу
- **THEN** он находит команду запуска агента с указанием профиля аккаунта и запускает агента,
  не обращаясь к другим документам

#### Scenario: Пользователю нужны подробности о работе с образом
- **WHEN** пользователю нужны сведения, выходящие за рамки запуска агента в этом наборе
- **THEN** `README.md` даёт ссылку на репозиторий базового образа, а не собственную копию его
  инструкции

#### Scenario: Образ обновился в реестре
- **WHEN** пользователь хочет получить актуальный образ окружения в уже настроенном проекте
- **THEN** `README.md` называет команду обновления образа, и пользователю не нужно выяснять её
  самостоятельно

### Requirement: Таблица переменных окружения полна относительно шаблона
`README.md` SHALL содержать таблицу переменных окружения, в которой присутствует каждая переменная
шаблона `.env.example`. Для каждой переменной таблица SHALL называть назначение и признак
обязательности: задаётся пользователем обязательно, задаётся по необходимости или имеет значение
по умолчанию из шаблона.

Переменные, задаваемые пользователем вручную и отсутствующие в шаблоне, SHALL присутствовать
в таблице с явной пометкой об этом.

Переменные канала к целям `make` на хосте SHALL входить в таблицу наравне с остальными: таблица
описывает файл окружения целиком, а не только переменные обязательного пути.

#### Scenario: Пользователь заполняет файл окружения
- **WHEN** пользователь открывает `.env` и сверяется с таблицей переменных в `README.md`
- **THEN** каждая переменная файла присутствует в таблице с назначением и признаком обязательности

#### Scenario: Пользователь настраивает канал к целям make
- **WHEN** пользователь настраивает раннер целей `make` на хосте
- **THEN** он находит в таблице переменные адреса, порта и токена раннера с их назначением

#### Scenario: В шаблон добавлена переменная
- **WHEN** в `.env.example` появляется новая переменная
- **THEN** её отсутствие в таблице `README.md` — расхождение документа с шаблоном, требующее правки

### Requirement: README не ведёт таблицу версий
`README.md` MUST NOT содержать таблицу версий документа. История правок документа ведётся системой
контроля версий, а таблица версий адресована сопровождающему репозиторий, а не пользователю набора,
для которого документ написан.

Норма SHALL быть зафиксирована исключением из
[DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа) по типу документа:
таблицу версий не ведёт ни один `README.md` репозитория. Прочие документы — правила, документ
для контрибьюторов — таблицу версий SHALL сохранять.

#### Scenario: Пользователь дочитывает README
- **WHEN** пользователь доходит до конца `README.md`
- **THEN** документ заканчивается содержательным разделом, а таблицы версий в нём нет

#### Scenario: Правка README
- **WHEN** в `README.md` вносится изменение
- **THEN** запись о нём в документ не добавляется, а источником истории остаётся система контроля
  версий

#### Scenario: Таблица версий в остальных документах
- **WHEN** изменение затрагивает документ правил или документ для контрибьюторов
- **THEN** таблица версий этого документа дополняется записью по
  [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа)
