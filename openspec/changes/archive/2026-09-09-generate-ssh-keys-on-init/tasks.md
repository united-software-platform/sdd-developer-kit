## 1. Подготовка цели init

- [x] 1.1 Вынести в цель `init` файла `Makefile` чтение `SSH_KEY`, `SSH_CONFIG`, `GIT_HOST`,
  `GIT_USER`, `CONTAINER_SSH_DIR` из `.env` способом из цели `shell` (`sed -n 's/^ИМЯ=//p' .env`)
  с умолчаниями из `.env.example`; проверка — `make init` в проекте с урезанным `.env` доходит
  до конца и использует умолчания
- [x] 1.2 Добавить в цель `init` проверку наличия `ssh-keygen` с завершением по ошибке и
  сообщением о недостающей утилите; проверка — запуск цели с недоступным `ssh-keygen`
  (например `PATH=/nonexistent make init`) печатает имя утилиты и возвращает ненулевой код

## 2. Генерация ключа

- [x] 2.1 Добавить в цель `init` генерацию пары ed25519 по пути `SSH_KEY` без passphrase, только
  когда приватного ключа нет; проверка — в проекте без ключа после `make init` появляются
  приватный и публичный файлы, а `ssh-keygen -y -f <SSH_KEY> -P ""` читает ключ без запроса
- [x] 2.2 Сохранить существующий ключ нетронутым и сообщить об этом; проверка — контрольная сумма
  приватного ключа до и после повторного `make init` совпадает, в выводе есть строка о том, что
  ключ оставлен как есть
- [x] 2.3 Выставить права `600` на приватный ключ и `644` на публичный; проверка —
  `stat -c '%a' <SSH_KEY> <SSH_KEY>.pub` возвращает `600` и `644`
- [x] 2.4 Печатать содержимое публичного ключа и указание добавить его в git-сервис только при
  создании новой пары; проверка — первый `make init` печатает ключ, повторный не печатает,
  приватный ключ в выводе не появляется ни разу

## 3. Конфигурация SSH

- [x] 3.1 Добавить в цель `init` создание файла по пути `SSH_CONFIG` с записью `Host`/`HostName`
  для `GIT_HOST`, `User` из `GIT_USER`, `IdentityFile` вида `<CONTAINER_SSH_DIR>/<имя файла
  SSH_KEY>` и `IdentitiesOnly yes`; проверка — при заданном `GIT_HOST` файл создан и `IdentityFile`
  содержит контейнерный путь, а не хостовый
- [x] 3.2 Пропускать создание конфигурации при пустом `GIT_HOST` с предупреждением о повторном
  запуске, не прерывая цель; проверка — `make init` с пустым `GIT_HOST` завершается кодом `0`,
  файла `SSH_CONFIG` нет, в выводе есть указание заполнить переменную и повторить `make init`
- [x] 3.3 Не изменять существующий файл конфигурации; проверка — контрольная сумма `SSH_CONFIG`
  до и после повторного `make init` совпадает
- [x] 3.4 Выставить права `644` на созданный файл конфигурации; проверка —
  `stat -c '%a' <SSH_CONFIG>` возвращает `644`

## 4. Итоговое сообщение и сквозная проверка

- [x] 4.1 Обновить завершающее сообщение цели `init`: убрать указание «положить SSH-ключ в `.ssh/`»,
  оставить заполнение `.env` и создание каталога профиля; проверка — вывод `make init` не содержит
  указания копировать ключ
- [x] 4.2 Пройти сценарий установки целиком в чистом каталоге: `make init` → заполнить `GIT_HOST`
  и `CLAUDE_PROFILE` → `make init` повторно → `make build` → `make up` → `make shell`; проверка —
  внутри контейнера `cat ~/.ssh/config` показывает запись для `GIT_HOST`, а `ls -l ~/.ssh` —
  ключ с правами `600`

## 5. Документация

- [x] 5.1 Обновить в `README.md` описание шага 2 «Подготовить проект»: перечислить создание ключа
  и `.ssh/config`; проверка — текст шага соответствует фактическому выводу `make init`
- [x] 5.2 Переписать в `README.md` шаг 4: вместо `cp` личного ключа — добавление напечатанного
  публичного ключа в git-сервис и повторный запуск `make init` после заполнения `GIT_HOST`;
  проверка — в шаге нет команды `cp` для ключей
- [x] 5.3 Добавить `openssh-client` (`ssh-keygen`) в предусловия установки `README.md`; проверка —
  перечень предусловий содержит утилиту
- [x] 5.4 Дополнить таблицу версий `README.md` записью по [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа)
  и [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа); проверка — новая
  строка добавлена последней и содержит задачу, агента и модель
