## 1. Подготовка

- [x] 1.1 Уточнить у пользователя код и номер задачи для коммитов ([GIT-010](../../../rules/git-rules.md#формат-сообщения-коммита)) и зафиксировать текущее staged-состояние `rules/not-adaptated/` отдельным коммитом — проверка: `git status --short` не показывает незакоммиченных правок в `rules/`, гейт [GIT-020](../../../rules/git-rules.md#гейт-редактирования) пройден
- [x] 1.2 Согласовать с пользователем формулировку `CLAR-005`, восстановленную по смыслу вместо недоступных `PHP-002`..`PHP-004` (design.md, решение 5) — проверка: формулировка подтверждена либо заменена предложенной пользователем. **Решение пользователя: `CLAR-005` удаляется целиком**, восстановление по смыслу отклонено

## 2. Адаптация `clean-architecture.md`

- [x] 2.1 Переписать блок примера с PHP на псевдокод в нотации `ddd.md`, заменив домен «требования» на «заказы»: интерфейс репозитория, сценарий использования, реализация репозитория — проверка: в файле не осталось ни одного блока ` ```php `, все блоки помечены языком по [DOC-016](../../../rules/documentation-rules.md#код-doc-016)
- [x] 2.2 Снять привязку к фреймворкам в `CLAR-004`, `CLAR-012`, `CLAR-016` и в описании слоя Infrastructure: Symfony, Doctrine, PDO, PSR-контейнер, `EntityManager` заменить ролевыми формулировками — проверка: `grep -n 'Symfony\|Doctrine\|PDO\|PSR' rules/clean-architecture.md` не выдаёт вхождений в действующем тексте
- [x] 2.3 Удалить `CLAR-005` из таблицы правил и из текста раздела «Domain» (решение пользователя, задача 1.2); нумерацию остальных `CLAR-*` сохранить, пропуск зафиксировать примечанием о неизменности кодов — проверка: в файле нет ссылок на коды `PHP-*` и на `php-style.md`, нумерация `CLAR-004` → `CLAR-006` не пересобрана
- [x] 2.4 Заменить требования к структуре каталогов (`CLAR-011`, `CLAR-022`, `CLAR-023`, раздел «Структура каталогов») описанием логических слоёв с оговоркой о свободе физического воплощения; схему направления зависимостей сохранить — проверка: в файле нет дерева `src/...`, а норма module-first по-прежнему сформулирована
- [x] 2.5 Снять языковые конструкции из формулировок `CLAR-008`, `CLAR-009`, `CLAR-014` и таблицы структуры UseCase (`final class`, `readonly`, сигнатура с типами) — проверка: каждая норма записана через наблюдаемое свойство (единственная точка входа, неизменяемость, отсутствие логики), суффиксы имён ролей сохранены
- [x] 2.6 Обнулить таблицу версий до одной записи `1.0.0` с датой, задачей, агентом и моделью по [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа) — проверка: таблица содержит ровно одну строку данных

## 3. Адаптация `cqrs.md`

- [x] 3.1 Переписать блок примера с PHP на псевдокод, сохранив домен «задачи» или переведя его на «заказы» для единообразия с `clean-architecture.md` — проверка: блоков ` ```php ` в файле нет, имена в примере согласованы с примером `clean-architecture.md`
- [x] 3.2 Заменить раздел «Структура каталогов» описанием принадлежности контрактов слоям (write — домен, read — приложение, реализации — инфраструктура) — проверка: в файле нет дерева `src/{Module}/...` и имён файлов с расширением языка
- [x] 3.3 Снять привязку реализаций к конкретной технологии доступа к данным (`Pdo*`, «напрямую из SQL») — проверка: норма разделения чтения и записи сформулирована без указания механизма хранения
- [x] 3.4 Обнулить таблицу версий до одной записи `1.0.0` — проверка: таблица содержит ровно одну строку данных

## 4. Адаптация `ddd.md`

- [x] 4.1 Убрать хвост ссылки на `php-style.md` и коды `PHP-004`, `PHP-008` из `DDD-024`, сохранив саму норму — проверка: `grep -n 'php-style\|PHP-0' rules/ddd.md` не выдаёт вхождений
- [x] 4.2 Проверить ссылку `DDD-020` на `clean-architecture.md` после переноса файлов: относительный путь должен вести на `./clean-architecture.md` и указывать на существующий якорь — проверка: файл и якорь существуют
- [x] 4.3 Снять из `DDD-024` языковые конструкции `new self`, `new static`, заменив их формулировкой через поведение — проверка: норма читается без знания синтаксиса конкретного языка
- [x] 4.4 Привести навигацию в соответствие с составом разделов: пункт «Версионирование» в навигации отсутствует, хотя раздел есть — проверка: каждый раздел второго уровня представлен в навигации
- [x] 4.5 Обнулить таблицу версий до одной записи `1.0.0` в формате `X.Y.Z` (текущая запись `1.0` формат нарушает) — проверка: таблица содержит ровно одну строку данных с версией `1.0.0`

## 5. Адаптация `docker.md`

- [x] 5.1 Переформулировать `DOCK-001`: источник префикса — имя проекта, без отсылки к заголовку README конкретной системы и к «первичным требованиям» методологии — проверка: правило выполнимо в проекте без README и без документа требований
- [x] 5.2 Переформулировать `DOCK-008`: диапазон публикуемых портов задаёт проект; норма — не использовать порты по умолчанию и проверять занятость — проверка: в тексте нет жёсткого диапазона `11000–12000`
- [x] 5.3 Обобщить `DOCK-005`, `DOCK-012`, `DOCK-013` и раздел «Пользователь backend-контейнера»: «backend-контейнер» → «контейнер приложения», пользователь и группа `www` → непривилегированный пользователь приложения, имя которого задаёт проект — проверка: `grep -n 'www\|backend\|php' rules/docker.md` не выдаёт вхождений в действующем тексте
- [x] 5.4 Переписать пример `Dockerfile` без базового образа конкретного языка и пример `docker-compose.yaml` на нейтральных именах сервисов вместо `rag-*`; заменить пример init-сервиса с Ollama на нейтральный сценарий наполнения тома — проверка: в примерах нет имён и образов конкретного проекта, нормы `DOCK-014` и `DOCK-015` по-прежнему проиллюстрированы
- [x] 5.5 Проверить, что нормы `DOCK-003`, `DOCK-004`, `DOCK-006`, `DOCK-007`, `DOCK-009`..`DOCK-011` остались неизменными по содержанию — проверка: состав правил `DOCK-001`..`DOCK-015` и их нумерация совпадают с исходными
- [x] 5.6 Обнулить таблицу версий до одной записи `1.0.0` в формате `X.Y.Z` — проверка: таблица содержит ровно одну строку данных

## 6. Перенос файлов

- [x] 6.1 Перенести четыре адаптированных документа из `rules/not-adaptated/` в `rules/` и удалить опустевший каталог — проверка: `ls rules/` показывает девять документов, каталога `not-adaptated` нет
- [x] 6.2 Проверить все относительные ссылки между перенесёнными документами и из них на действующие правила набора — проверка: каждая ссылка ведёт на существующий файл и существующий якорь

## 7. Подключение к точкам входа

- [x] 7.1 Добавить в `CLAUDE.md` четыре `@`-импорта и четыре строки списка в разделе «Правила кода» — проверка: перечень подключённых документов совпадает с составом `rules/`
- [x] 7.2 Добавить те же четыре документа в `AGENTS.md` списком ссылок одной правкой с 7.1 — проверка: перечни `CLAUDE.md` и `AGENTS.md` совпадают, требование `repo-rules` «Точки входа для агентов перечисляют один и тот же свод» выполнено
- [x] 7.3 Дописать записи в таблицы версий `CLAUDE.md` и `AGENTS.md` по [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа) — проверка: новая запись стоит последней строкой в обеих таблицах

## 8. Приёмка

- [x] 8.1 Сверить каждый из четырёх документов с требованиями спеки `repo-rules`: нет кода на конкретном языке, нет привязки к фреймворку и структуре каталогов, примеры из общеизвестных доменов, история версий обнулена — проверка: по каждому требованию спеки назван фрагмент документа, который ему удовлетворяет
- [x] 8.2 Проверить сохранность проверяемости норм: для каждого правила сформулировать, какой фрагмент кода его нарушает (design.md, раздел Risks) — проверка: правил, потерявших проверяемость после адаптации, не осталось
- [x] 8.3 Показать пользователю состав коммита и предложенное сообщение, дождаться подтверждения ([GIT-016](../../../rules/git-rules.md#коммиты-и-push)) — проверка: коммит создан только после явного подтверждения
