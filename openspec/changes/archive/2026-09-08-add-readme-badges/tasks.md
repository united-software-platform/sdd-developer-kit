## 1. Проверка предпосылки

- [x] 1.1 Проверить анонимную доступность источников: запросить
  `https://raw.githubusercontent.com/united-software-platform/sdd-developer-kit/main/Dockerfile`
  и `.../main/VERSION` без авторизации; проверка пройдена, если обе отдают `200` с содержимым файла
- [x] 1.2 Если хотя бы один источник отдал `404`, зафиксировать это в
  [`design.md`](./design.md) и перейти на статические значения версий; проверка — в `design.md`
  записано принятое решение и причина

## 2. Сборка URL бейджей

- [x] 2.1 Собрать пять URL динамических бейджей по таблице из [`design.md`](./design.md)
  (образ, Python, OpenSpec, uv, версия kit'а) с percent-кодированием параметров `url` и `search`;
  проверка — каждый URL, открытый в браузере, отдаёт бейдж со значением, совпадающим с текущим
  содержимым `Dockerfile` (`ghcr.io/united-software-platform/claude:latest`, `3.12`, `1.11.0`,
  `0.12.7`) и `VERSION` (`1.0.0`)
- [x] 2.2 Собрать статический URL бейджа лицензии `license-MIT`; проверка — бейдж отображает `MIT`,
  что совпадает с заголовком файла [`LICENSE`](../../../LICENSE)

## 3. Блок бейджей в README

- [x] 3.1 Вставить блок бейджей в [`README.md`](../../../README.md) между заголовком
  `# SDD Developer Kit` и абзацем назначения; проверка — блок стоит именно там, ни один
  существующий раздел не сдвинут по смыслу
- [x] 3.2 Оформить каждый бейдж как `[![alt](url-изображения)](url-ссылки)` с alt-текстом,
  называющим характеристику и файл-источник её значения (само значение в alt не выносится); проверка — в блоке нет HTML-тегов ([DOC-005](../../../rules/documentation-rules.md#разметка-markdown)),
  каждый бейдж имеет непустой alt
- [x] 3.3 Проставить ссылки бейджей: образ — на страницу пакета в GHCR (абсолютный URL), Python,
  OpenSpec и uv — на `./Dockerfile`, версия — на `./VERSION`, лицензия — на `./LICENSE`; проверка —
  все внутренние цели записаны относительными путями по [DOC-015](../../../rules/documentation-rules.md#ссылки-doc-015)
  и открываются из отрендеренного `README.md`

## 4. Документирование состава

- [x] 4.1 Добавить в [`README.md`](../../../README.md) раздел «Бейджи» с таблицей
  «бейдж ↔ файл-источник ↔ `ARG` или файл»; проверка — по таблице для каждого из
  `UV_VERSION`, `OPENSPEC_VERSION`, `PYTHON_VERSION` и строки `FROM` определяется зависящий бейдж.
  Раздел размещается в `README.md`, а не в `docs/sdd-kit.md`: последний входит в `PAYLOAD_PATHS`
  установщика и уехал бы в целевые проекты
- [x] 4.2 Зафиксировать в том же разделе ограничения: зависимость от анонимной доступности
  репозитория, привязка значений к ветке `main`, поломка бейджа при переименовании `ARG`;
  проверка — все три ограничения названы явно по [DOC-020](../../../rules/documentation-rules.md#полнота-содержания)
- [x] 4.3 Добавить пункт нового раздела в навигацию `README.md`; проверка — ссылка навигации
  ведёт на существующий якорь

## 5. Завершение

- [x] 5.1 Дописать запись в таблицу версий `README.md` по
  [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа) и
  [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа); проверка — новая
  запись последняя в таблице, колонки «Задача», «Агент», «Модель» заполнены
- [x] 5.2 Проверить сценарии спецификации на итоговом `README.md`: значения всех пяти динамических
  бейджей совпадают с содержимым `Dockerfile` и `VERSION` в `main`, а в разметке `README.md` эти
  значения не встречаются ни разу (`grep -n '3\.12\|1\.11\.0\|0\.12\.7' README.md` не находит
  их вне таблицы версий); при отключённой загрузке изображений вместо каждого бейджа остаётся
  читаемый alt-текст
- [x] 5.3 Выполнить `openspec validate add-readme-badges --strict`; проверка — команда завершается
  без ошибок
