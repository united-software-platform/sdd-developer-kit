## 1. Состав поставки

- [x] 1.1 Добавить `tools/make-lint` в перечень `PAYLOAD_PATHS` файла `install.sh`; проверить
  прогоном `sh install.sh --dry-run --source <клон kit'а>` во временном пустом каталоге, что
  в плане присутствуют строки `CREATE tools/make-lint/check.sh` и файлы каталога `rules/`
- [x] 1.2 Дополнить комментарий к `PAYLOAD_PATHS` границей поставки: проверка входит, потому что
  её предписывает переносимый документ правил; пайплайн репозитория не входит, потому что сценарии
  системы непрерывной интеграции целевой проект ведёт сам. Проверить чтением, что комментарий
  не противоречит новому перечню
- [x] 1.3 Выполнить установку в пустой временный каталог (`sh install.sh --source <клон kit'а> -y`)
  и убедиться, что `tools/make-lint/check.sh` на месте, исполним и `sh tools/make-lint/check.sh`
  в этом каталоге даёт вердикт по норме 30 строк, не обращаясь к репозиторию kit'а
- [x] 1.4 Проверить, что установка не создала в целевом каталоге `.github/` и файла
  `makefile-lint.yml`, и что путь `tools/make-lint` попал в манифест `.sdd-kit-manifest.json`
  с контрольной суммой

## 2. Документ правил

- [x] 2.1 Перевести назначение `rules/makefile-rules.md` и формулировки `MAKE-001`..`MAKE-004`
  с «`Makefile` репозитория kit'а» на «`Makefile` проекта»; проверить чтением, что ни одна
  действующая формулировка не привязывает норму к репозиторию kit'а
- [x] 2.2 Убрать из `MAKE-005` относительную ссылку на `.github/workflows/makefile-lint.yml`,
  сохранив храповик как правило репозитория kit'а в текстовой формулировке; проверить, что
  в документе не осталось относительных ссылок на файлы вне состава поставки
- [x] 2.3 Пройти по всем относительным ссылкам `rules/makefile-rules.md` в каталоге установки
  из задачи 1.3 и убедиться, что каждая ведёт на существующий там файл
- [x] 2.4 Дописать запись в таблицу версий `rules/makefile-rules.md` по
  [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа) и
  [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа): номер версии,
  дата, задача — OpenSpec change `deliver-make-lint`, агент, модель, описание изменений;
  проверить, что запись добавлена последней строкой

## 3. Скрипт проверки

- [x] 3.1 Исправить шапку `tools/make-lint/check.sh`: утверждение «В состав поставки скрипт
  не входит» заменить описанием нового положения — скрипт входит в поставку и работает в том
  проекте, где лежит; проверить чтением, что комментарий соответствует составу поставки
- [x] 3.2 Обобщить формулировку справки «`Makefile` в корне репозитория» до корня проекта;
  проверить выводом `sh tools/make-lint/check.sh --help`
- [x] 3.3 Убедиться, что поведение не изменилось: `sh tools/make-lint/check.sh` в репозитории kit'а
  завершается успехом, `sh tools/make-lint/check.sh --max-lines 0` — ошибкой вызова с кодом 2,
  а `--makefile` с несуществующим путём — ошибкой окружения

## 4. Документация репозитория

- [x] 4.1 Добавить в перечень состава набора `README.md` пункт проверки объёма секций `Makefile`
  с относительной ссылкой на `./tools/make-lint`, описанием назначения и способа запуска;
  проверить, что пункт умещается в одну-две строки, как прочие
- [x] 4.2 Добавить в таблицу «Что попадает в проект» строку с компонентом проверки и расположением
  `tools/make-lint/`; проверить, что таблица не превратилась во второй перечень состава и ссылается
  на раздел состава набора
- [x] 4.3 Проверить, что раздел состава `README.md` явно называет границу: пайплайны репозитория
  kit'а в состав набора не входят и описаны отдельно
- [x] 4.4 Проверить, что `CONTRIBUTING.md` и `.github/workflows/makefile-lint.yml` не требуют
  правок: оба остаются принадлежностью репозитория kit'а, пути вызова проверки не изменились

## 5. Приёмка

- [x] 5.1 Выполнить `sh tools/make-lint/check.sh` в репозитории kit'а по
  [MAKE-004](../../../rules/makefile-rules.md#локальный-прогон-проверки-make-004) и убедиться,
  что вердикт — успех (`Makefile` изменением не затрагивается, прогон подтверждает это)
- [x] 5.2 Выполнить `openspec validate deliver-make-lint --strict` и убедиться, что change валиден
- [x] 5.3 Повторить установку поверх каталога из задачи 1.3 и убедиться, что план не содержит
  конфликтов по `tools/make-lint/check.sh`, а изменённый вручную файл поставки распознаётся
  как конфликт
