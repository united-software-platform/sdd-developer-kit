## 1. Запуск сборки образа релизом

- [x] 1.1 Добавить в `.github/workflows/release.yml` право `actions: write` рядом с `contents: write`
      и проверить по файлу, что других прав заданию не добавлено
- [x] 1.2 Добавить в `release.yml` последним шагом — после `git push origin HEAD` и публикации тега —
      запуск `gh workflow run docker-agent.yml --ref main` с `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`
      и комментарием, объясняющим, почему push релизного коммита прогона не порождает и почему
      ссылка запуска — `main`, а не тег; проверить порядок шагов чтением файла
- [x] 1.3 Убедиться, что шаг не помечен `continue-on-error`: неуспех самой диспетчеризации обязан
      красить релиз (проверяется чтением файла)

## 2. Версия kit'а в образе

- [x] 2.1 Передать метки `org.opencontainers.image.version` (из `needs.resolve.outputs.kit_version`),
      `org.opencontainers.image.revision` (`github.sha`) и `org.opencontainers.image.source`
      в сборку задания `check` в `.github/workflows/docker-agent.yml`
- [x] 2.2 Передать те же метки с теми же значениями в сборку задания `push`; сверить оба списка
      построчно — расхождение означало бы, что проверяется не то, что публикуется
- [x] 2.3 Дополнить шаг `Запуск инструментов образа` сверкой метки версии с `resolve.kit_version`
      через `docker inspect` — по образцу действующей сверки версии OpenSpec, с сообщением об ошибке,
      называющим оба значения

## 3. Документация

- [x] 3.1 В `README.md`, раздел «Образ окружения агента», назвать релиз среди событий пересборки
      и описать, как узнать версию образа по метке (`docker inspect`); зафиксировать ограничение —
      релиз сборку запускает, но её результата не ждёт, и до её завершения `latest` указывает
      на предыдущую версию
- [x] 3.2 Добавить строку в таблицу версий `README.md` по [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа)
      и [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа): запись дописывается
      в конец таблицы, задача — OpenSpec change `build-image-on-release`

## 4. Проверка

- [x] 4.1 Прогнать `openspec validate build-image-on-release --strict` и убедиться, что change валиден
- [ ] 4.2 После слияния в `main`: убедиться, что прогон `docker-agent` прошёл, а `docker inspect`
      опубликованного образа возвращает метку версии, совпадающую с содержимым `VERSION`
- [ ] 4.3 Если тега текущей версии в реестре нет — запустить `docker-agent` вручную по `main`
      и подтвердить появление тега через `docker buildx imagetools inspect`
- [ ] 4.4 На ближайшем релизе убедиться, что прогон `docker-agent` стартовал из релизного запуска
      и опубликовал образ с тегом новой версии и `latest`
