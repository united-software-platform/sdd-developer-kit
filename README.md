# SDD Developer Kit

[![базовый образ Claude из Dockerfile](https://img.shields.io/badge/dynamic/regex?url=https%3A%2F%2Fraw.githubusercontent.com%2Funited-software-platform%2Fsdd-developer-kit%2Fmain%2FDockerfile&search=FROM%20%28ghcr%5C.io%2F%5CS%2B%2Fclaude%3A%5CS%2B%29&replace=%241&label=claude%20image&color=2496ED&logo=docker)](https://github.com/orgs/united-software-platform/packages/container/package/claude)
[![версия Python из Dockerfile](https://img.shields.io/badge/dynamic/regex?url=https%3A%2F%2Fraw.githubusercontent.com%2Funited-software-platform%2Fsdd-developer-kit%2Fmain%2FDockerfile&search=ARG%20PYTHON_VERSION%3D%28%5B%5Cd.%5D%2B%29&replace=%241&label=python&color=3776AB&logo=python)](./Dockerfile)
[![версия OpenSpec из Dockerfile](https://img.shields.io/badge/dynamic/regex?url=https%3A%2F%2Fraw.githubusercontent.com%2Funited-software-platform%2Fsdd-developer-kit%2Fmain%2FDockerfile&search=ARG%20OPENSPEC_VERSION%3D%28%5B%5Cd.%5D%2B%29&replace=%241&label=openspec&color=5B4FCF)](./Dockerfile)
[![версия uv из Dockerfile](https://img.shields.io/badge/dynamic/regex?url=https%3A%2F%2Fraw.githubusercontent.com%2Funited-software-platform%2Fsdd-developer-kit%2Fmain%2FDockerfile&search=ARG%20UV_VERSION%3D%28%5B%5Cd.%5D%2B%29&replace=%241&label=uv&color=DE5FE9)](./Dockerfile)
[![версия kit'а из файла VERSION](https://img.shields.io/badge/dynamic/regex?url=https%3A%2F%2Fraw.githubusercontent.com%2Funited-software-platform%2Fsdd-developer-kit%2Fmain%2FVERSION&search=%28%5B%5Cd.%5D%2B%29&replace=%241&label=version&color=007EC6)](./VERSION)
[![лицензия MIT](https://img.shields.io/badge/license-MIT-007EC6)](./LICENSE)
Изолированное окружение разработки для Spec-Driven Development (SDD): контейнер с Claude Code
и предустановленным OpenSpec, правила репозитория и инструменты SDD — разворачиваются в любом
проекте одной командой.

---

## Навигация

- [Установка](#установка)
- [Порядок установки](#порядок-установки)
- [Что попадает в проект](#что-попадает-в-проект)
- [Команды](#команды)
- [Переменные окружения](#переменные-окружения)
- [Опционально: host-runner](#опционально-host-runner)
- [Повторная установка и обновление](#повторная-установка-и-обновление)
- [Ограничения](#ограничения)
- [Разработка самого kit'а](#разработка-самого-kitа)
- [Лицензия](#лицензия)
- [Версионирование](#версионирование)

---

## Установка

В корне своего проекта:

```bash
curl -fsSL https://raw.githubusercontent.com/united-software-platform/sdd-developer-kit/main/install.sh | sh
```

Дальше — по шагам из раздела [Порядок установки](#порядок-установки): подготовка проекта,
заполнение `.env`, сборка образа, запуск контейнера.

Предусловия: Docker с плагином Compose, `make`, `ssh-keygen` (пакет `openssh-client`),
`curl` или `wget`, доступ в интернет.

Если запускать чужой скрипт из сети не хочется, его можно сначала прочитать:

```bash
curl -fsSL -O https://raw.githubusercontent.com/united-software-platform/sdd-developer-kit/main/install.sh
less install.sh
sh install.sh
```

Установщик не пишет ничего, пока не покажет план: файлы к созданию, к обновлению и к перезаписи.
Перезапись существующих файлов требует подтверждения.

---

## Порядок установки

Шаги выполняются в корне вашего проекта по порядку.

**Шаг 1. Установить инструменты**

```bash
curl -fsSL https://raw.githubusercontent.com/united-software-platform/sdd-developer-kit/main/install.sh | sh
```

Файлы kit'а появляются в корне проекта. Если какие-то из них там уже есть, установщик покажет
их перечень и спросит подтверждение перед перезаписью.

**Шаг 2. Подготовить проект**

```bash
make init
```

Создаёт `.env` из шаблона, каталоги `.ssh/` и `.claude-accounts/` и дописывает их в `.gitignore`,
чтобы секреты не попали в репозиторий. Затем готовит SSH: генерирует ключ `ed25519` без passphrase
по пути из `SSH_KEY` и печатает публичный ключ. Конфигурацию `.ssh/config` цель создаёт по
заполненному `GIT_HOST` — на этом шаге он ещё пуст, поэтому цель сообщает о повторном запуске.

Цель ничего не перезаписывает: существующие `.env`, ключ и `.ssh/config` остаются как есть,
поэтому её можно запускать повторно.

**Шаг 3. Заполнить `.env`**

Открыть `.env` и задать значения. Обязательны:

- `GIT_HOST` — хост git-сервиса (например `github.com`);
- `CLAUDE_PROFILE` — имя каталога профиля аккаунта, например `alex`. Переменной нет в шаблоне:
  её значение своё у каждого разработчика.

**Шаг 4. Зарегистрировать ключ и создать профиль**

```bash
make init
mkdir -p .claude-accounts/$(sed -n 's/^CLAUDE_PROFILE=//p' .env)
```

Повторный `make init` создаёт `.ssh/config`: `GIT_HOST` теперь заполнен. Публичный ключ он уже
не печатает — вывод даётся один раз, при создании ключа на шаге 2, поэтому вывести его заново:

```bash
cat .ssh/id_ed25519.pub
```

Этот ключ нужно добавить в git-сервис. Он сгенерирован для проекта и сервису пока неизвестен —
без регистрации push из контейнера не пройдёт.

**Шаг 5. Собрать образ**

```bash
make build
```

Образ `claude-openspec:local` собирается локально; готового образа с таким тегом в реестре нет.

**Шаг 6. Запустить контейнер**

```bash
make up
```

**Шаг 7. Войти в контейнер**

```bash
make shell
```

Внутри доступны Claude Code и OpenSpec: `openspec status`, `/opsx:propose` и остальные команды.
Установка окончена.

---

## Что попадает в проект

| Компонент | Расположение |
|-----------|--------------|
| Окружение агента | `Dockerfile`, `docker-compose.yml`, `Makefile`, `.env.example` |
| Правила репозитория | `rules/`, `CLAUDE.md`, `AGENTS.md` |
| Инструменты SDD | `.claude/skills/`, `.claude/commands/`, `openspec/config.yaml` |
| Раннер целей сборки | `tools/host-runner/` |
| Манифест установки | `.sdd-kit-manifest.json` |

---

## Команды

| Команда | Действие |
|---------|----------|
| `make init` | Подготовка проекта: `.env`, каталоги, SSH-ключ и `.ssh/config`, записи в `.gitignore` |
| `make build` | Сборка образа |
| `make up` | Запуск контейнера агента |
| `make shell` | Вход в контейнер |
| `make down` | Остановка контейнера |
| `make help` | Список команд с описаниями |

Прямые вызовы `docker compose` не нужны ни на одном шаге. В установленном проекте тот же список
всегда доступен по `make help`.

---

## Переменные окружения

Актуальный состав — в [`.env.example`](./.env.example). Кратко:

| Переменная | Назначение |
|------------|------------|
| `PROJECT_DIR` | Путь монтирования рабочего дерева проекта внутри контейнера |
| `CLAUDE_CONFIG_DIR` | Каталог конфигурации Claude Code внутри контейнера |
| `CLAUDE_ACCOUNTS_DIR` | Каталог на хосте, где лежат профили аккаунтов |
| `CLAUDE_PROFILE` | Имя каталога профиля аккаунта; задаётся вручную, в шаблон не входит |
| `CONTAINER_SSH_DIR` | Каталог SSH внутри контейнера |
| `SSH_DIR` | Каталог SSH на хосте, монтируемый в контейнер |
| `SSH_KEY`, `SSH_CONFIG`, `SSH_KNOWN_HOSTS` | Пути к файлам SSH-конфигурации |
| `GIT_HOST`, `GIT_USER` | Параметры доступа к git-хосту |
| `USER_ID`, `GROUP_ID` | UID/GID пользователя контейнера |

> **Важно:** контейнер видит только каталог профиля, заданного `CLAUDE_PROFILE`. Без этой
> переменной монтируется несуществующий каталог — так сделано намеренно, чтобы агент не получил
> доступ к чужим профилям.

---

## Опционально: host-runner

Внутри контейнера нет ни `make`, ни Docker-сокета, поэтому агент не может сам собрать образ или
перезапустить контейнер. Если это нужно, на хосте запускается host-runner — узкий канал к
разрешённым целям `make`. Подробности в [`tools/host-runner/README.md`](./tools/host-runner/README.md).

Шаг необязательный: без host-runner всё остальное работает, а вызовы целей `make` из контейнера
просто завершаются ошибкой соединения.

---

## Повторная установка и обновление

Повторный запуск команды установки обновляет файлы kit'а. Установщик сверяет их с манифестом
`.sdd-kit-manifest.json`:

- файл не менялся с момента установки — обновляется молча;
- файл изменён вами — попадает в перечень перезаписи и требует подтверждения.

Ключи установщика (при запуске через конвейер передаются как `| sh -s -- КЛЮЧ`):

| Ключ | Действие |
|------|----------|
| `--dry-run` | Показать план и ничего не записывать |
| `--yes` | Не спрашивать подтверждения (для CI) |
| `--ref <REF>` | Установить конкретную ветку, тег или коммит вместо `main` |
| `--source <DIR>` | Взять состав поставки из локального каталога вместо загрузки |

---

## Ограничения

- **Установка требует сети и публичного репозитория.** Работа в закрытом контуре и установка из
  приватного репозитория не поддерживаются; обходной путь — `--source` с локальной копией kit'а.
- **Слияния файлов нет.** Конфликт разрешается перезаписью целиком: свои цели `Makefile` или разделы
  `CLAUDE.md` нужно перенести вручную после установки.
- **Сбой во время записи оставляет проект частично установленным.** Транзакционного отката нет;
  признак незавершённой установки — отсутствующий или неполный манифест, лечение — повторный запуск.
- **Умолчание `main` невоспроизводимо во времени.** Для повторяемой установки указывайте `--ref`
  с тегом или коммитом.
- **Пути поставки без пробелов.** Список путей и манифест разбираются построчно.

---

## Разработка самого kit'а

Репозиторий kit'а сам развёрнут теми же инструментами: правила лежат в [`rules/`](./rules),
процесс ведётся через OpenSpec в [`openspec/`](./openspec), установщик — [`install.sh`](./install.sh).
Правила работы с репозиторием описаны в [`CLAUDE.md`](./CLAUDE.md) и [`AGENTS.md`](./AGENTS.md).

---

## Лицензия

[MIT](./LICENSE).

---

## Версионирование

| Версия | Дата | Задача | Агент | Модель | Описание изменений |
|--------|------|--------|-------|--------|--------------------|
| 1.0.0 | 2026-09-06 | OpenSpec change `repo-bootstrap-process` | Claude Code | Claude Sonnet 5 | Начальное создание: формализован бутстрап-процесс — предусловия (`.env`, SSH-ключ, профиль аккаунта), сборка образа, запуск контейнера, готовность к рабочей сессии, опциональный host-runner, таблица переменных окружения |
| 1.0.1 | 2026-09-08 | OpenSpec change `install-kit-into-project` | Claude Code | Claude Opus 5 | Добавлен раздел «Установка kit'а в целевой проект» со ссылками на установочный скрипт и документ окружения; соответствующий пункт добавлен в навигацию |
| 1.0.2 | 2026-09-08 | OpenSpec change `add-readme-badges` | Claude Code | Claude Opus 5 | Добавлен блок бейджей между заголовком и назначением по исключению [DOC-015](./rules/documentation-rules.md#ссылки-doc-015): базовый Claude-образ, Python, OpenSpec, uv, версия kit'а и лицензия. Пять бейджей динамические — значения читаются из `Dockerfile` и `VERSION` через `shields.io`, ручных копий версий в документе нет. Добавлен раздел «Бейджи» с таблицей «бейдж ↔ источник» и ограничениями, добавлен пункт навигации |
| 1.1.0 | 2026-09-08 | OpenSpec change `install-kit-into-project` | Claude Code | Claude Opus 5 | Кардинальная переработка: файл переведён из описания бутстрапа клона в витрину продукта — установка одной удалённой командой, состав поставки, таблица команд `make`, раздел о разработке самого kit'а. Порядок шагов перенесён в единственную инструкцию `SDD-KIT.md` и здесь не дублируется; описание окружения из `docs/sdd-kit.md` и документация установщика из `tools/install/README.md` объединены там же |
| 1.2.0 | 2026-09-08 | OpenSpec change `install-kit-into-project` | Claude Code | Claude Opus 5 | Документация сведена в один файл: из `SDD-KIT.md` перенесены порядок установки, переменные окружения, host-runner, повторная установка с ключами установщика и ограничения; `SDD-KIT.md` удалён вместе со ссылками на него. В состав поставки вместо инструкции добавлен манифест установки, в таблицу команд — `make help` |
| 1.2.1 | 2026-09-09 | OpenSpec change `generate-ssh-keys-on-init` | Claude Code | Claude Opus 5 | Порядок установки приведён в соответствие с расширенной целью `make init`: шаг 2 описывает генерацию SSH-ключа и создание `.ssh/config`, шаг 4 вместо копирования личного ключа командой `cp` описывает регистрацию сгенерированного публичного ключа в git-сервисе и повторный запуск `make init` после заполнения `GIT_HOST`. В предусловия добавлен `ssh-keygen` (пакет `openssh-client`): цель выполняется на хосте. Описание `make init` в таблице команд синхронизировано |
