## 1. Подготовка рабочего дерева

- [x] 1.1 Пройти гейт [GIT-020](../../../rules/git-rules.md#гейт-редактирования) по `README.md`
  и `rules/documentation-rules.md`: `git status -- README.md rules/documentation-rules.md`,
  `git diff` и `git diff --cached` по тем же путям; при незакоммиченных правках запросить у
  пользователя код, номер и описание задачи и закоммитить их до начала работы. Проверка: оба файла
  чисты в `git status` перед первой правкой

## 2. Расширение исключения DOC-015

- [x] 2.1 В `rules/documentation-rules.md` переформулировать границы исключения DOC-015: вместо
  одного места («только в блоке бейджей `README.md`») перечислить два — блок бейджей между
  заголовком и назначением и названия пунктов перечня состава, где цель ссылки внешняя; запрет HTML
  по DOC-005 и требование относительной ссылки на файл внутри репозитория оставить дословно.
  Проверка: раздел «Ссылки [DOC-015]» называет оба места, других изменений в файле нет
- [x] 2.2 Дописать строку в таблицу версий `rules/documentation-rules.md` по
  [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа) и
  [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа): версия `1.0.4`,
  дата, задача — OpenSpec change `add-readme-toolset-section`, агент, модель, описание правки.
  Проверка: новая строка — последняя в таблице, номер версии больше предыдущего

## 3. Раздел состава в README

- [x] 3.1 Добавить в `README.md` раздел «Состав набора» сразу после навигации, до раздела
  «Установка»: маркированный список из восьми пунктов, разбитый на две группы с вводными фразами —
  инструменты образа (`claude`, OpenSpec, Python, uv) и то, что появляется в проекте (окружение
  агента, правила репозитория, host-runner, skills и команды SDD). Проверка: список плоский, все
  восемь пунктов на месте, каждый умещается в одну-две строки
- [x] 3.2 Проставить ссылки в названиях пунктов: `https://github.com/united-software-platform/claude`,
  `https://github.com/Fission-AI/OpenSpec`, `https://www.python.org`,
  `https://github.com/astral-sh/uv`, относительные `./rules` и `./tools/host-runner`; окружение
  агента и skills оставить без ссылки — отдельного источника у них нет. Проверка: каждая ссылка
  открывается, относительные ведут на существующие пути
- [x] 3.3 Написать описания по `design.md`: зачем элемент нужен и как используется. У `claude`
  и OpenSpec — сжатая формулировка их репозиториев, сверенная на момент правки; у Python —
  «Расширяет возможности агента», у uv — «Менеджер зависимостей для Python»; у элементов второй
  группы — их файлы и каталоги плюс способ применения; host-runner описать через то, что он
  позволяет сделать («запускать команды `make` на вашей машине»), без внутренних понятий вроде
  белого списка целей. Проверка: ни одно описание не пересказывает название, не опирается
  на понятия из профильных разделов и не приписывает инструменту свойств, которых источник
  не называет
- [x] 3.4 Назвать пункт образа `claude` — как в бейдже шапки и в `FROM` `Dockerfile`. Проверка:
  название пункта совпадает с именем образа
- [x] 3.5 Зафиксировать в тексте раздела границу перечня и место версий по
  [DOC-020](../../../rules/documentation-rules.md#полнота-содержания): собственные проверки
  репозитория (`tools/make-lint/`) в состав набора не входят и описаны в разделе «Разработка самого
  kit'а»; версии показывает блок бейджей, а их источники — `Dockerfile` и `VERSION`. Проверка: обе
  оговорки присутствуют, номеров версий в разделе нет
- [x] 3.6 Добавить пункт нового раздела в навигацию `README.md` на позицию перед «Установка».
  Проверка: якорь пункта совпадает с заголовком раздела и переход по нему работает

## 4. Разгрузка существующих разделов

- [x] 4.1 В таблице раздела «Образ окружения агента» заменить перечень в строке «Состав» ссылкой
  на раздел состава. Проверка: перечня инструментов в разделе больше нет, ссылка ведёт на новый
  раздел
- [x] 4.2 Во вводных фразах разделов «Что попадает в проект» и «Опционально: host-runner» добавить
  ссылку на раздел состава, сохранив таблицы расположений как есть. Проверка: разделы не повторяют
  перечень, таблицы «Компонент — Расположение» не изменились

## 5. Завершение

- [x] 5.1 Дописать строку в таблицу версий `README.md` по DOC-022 и DOC-024: следующая версия,
  дата, задача — OpenSpec change `add-readme-toolset-section`, агент, модель, описание изменений.
  Проверка: новая строка — последняя в таблице
- [x] 5.2 Сверить итог с требованиями: `openspec validate "add-readme-toolset-section" --strict`
  проходит, и по каждому требованию `readme-toolset` и `readme-badges` в `README.md`
  и `rules/documentation-rules.md` есть соответствующий фрагмент. Проверка: команда завершается
  без ошибок, расхождений по требованиям не осталось
- [x] 5.3 Показать пользователю состав изменений и предложенное сообщение коммита по
  [GIT-016](../../../rules/git-rules.md#коммиты-и-push), запросив код, номер и описание задачи;
  коммит выполнить только после явного подтверждения. Проверка: коммит создан после подтверждения
  либо изменения оставлены незакоммиченными по решению пользователя
