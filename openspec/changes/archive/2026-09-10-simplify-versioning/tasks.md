## 1. Перечень путей поставки возвращается в установщик

- [x] 1.1 В `install.sh` заменить чтение `payload.txt` на inline-перечень `PAYLOAD_PATHS`
      с прежним составом путей: `docker-compose.yml`, `Makefile`, `.env.example`, `CLAUDE.md`,
      `AGENTS.md`, `rules`, `tools/host-runner`. Комментарий блока переписать: причина явного
      списка сохраняется, упоминание отдельного файла и второго читателя удаляется; добавляется
      оговорка, что пути не содержат пробелов и обходятся словоделением.
      Проверка: `sh -n install.sh` завершается кодом `0`, `grep -n "payload\.txt" install.sh` ничего
      не находит
- [x] 1.2 Сохранить прежнее поведение обхода: отсутствующий путь прерывает установку через `die`,
      каталог разворачивается в перечень файлов через `find`, итоговый список сортируется и пустым
      быть не может. Проверка: предварительный просмотр установки во временном каталоге
      (`sh install.sh --dry-run` или соответствующий ключ скрипта) печатает тот же перечень файлов,
      что и до правки — списки совпадают дословно
- [x] 1.3 Удалить `payload.txt` из корня репозитория. Проверка: файла нет,
      `grep -rn "payload.txt" . --exclude-dir=.git --exclude-dir=archive` находит упоминания только
      в артефактах этого change'а и в архиве OpenSpec

## 2. Удаление сторожевой проверки версии

- [x] 2.1 Удалить `.github/workflows/version-check.yml`. Проверка: файла нет, в
      `.github/workflows/` остаются `docker-agent.yml`, `makefile-lint.yml`, `release.yml`
- [x] 2.2 Удалить `tools/version/check.sh` и пустой каталог `tools/version/`. Проверка: каталога
      нет, в `tools/` остаются `host-runner/` и `make-lint/`
- [x] 2.3 Убедиться, что на удалённые файлы не осталось действующих ссылок. Проверка:
      `grep -rn "tools/version\|version-check" . --exclude-dir=.git` находит упоминания только
      в архиве OpenSpec, в артефактах этого change'а и в незавершённом `revise-kit-composition`

## 3. Релиз по одному параметру

- [x] 3.1 В `.github/workflows/release.yml` удалить входной параметр `task` и шаг «Проверка кода
      задачи». Единственным вводом остаётся `bump` со значениями `patch`, `minor`, `major`.
      Проверка: файл разбирается как YAML без ошибок
      (`python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"`),
      `grep -n "task" .github/workflows/release.yml` ничего не находит
- [x] 3.2 Переписать шаг записи версии: заголовок коммита — `Релиз v$NEXT`, тело — `Версия повышена:
      $CURRENT -> $NEXT`. Шаги разбора версии, гейта незакоммиченных правок, создания тега `vX.Y.Z`
      и публикации оставить без изменений. Проверка: в файле нет подстановок `${{ }}` внутри текста
      команд — входные значения по-прежнему передаются через `env`
- [x] 3.3 Обновить головной комментарий `release.yml`: упоминание явно заданной задачи заменить
      описанием единственного ввода и ссылкой на исключение в правилах Git. Проверка: комментарий
      не ссылается на несуществующий параметр

## 4. Исключение для релизного коммита в правилах Git

- [x] 4.1 В `rules/git-rules.md` добавить в таблицу правил и в раздел «Формат сообщения коммита»
      исключение: машинный релизный коммит, создаваемый релизным сценарием и изменяющий только
      `VERSION`, освобождается от требования `{CODE}-NNN` в заголовке. Формулировка привязывается
      к автору, составу изменения и форме заголовка `Релиз vX.Y.Z`. Проверка: в разделе «Проверка
      сообщения коммита» появляется оговорка о том, к каким коммитам чек-лист не применяется
- [x] 4.2 Дописать строку в таблицу версий `rules/git-rules.md` по
      [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа) и
      [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа): версия `1.3.1`,
      дата, задача — OpenSpec change `simplify-versioning`, агент, модель. Проверка: новая строка
      последняя в таблице, номер версии больше предыдущего

## 5. Документация

- [x] 5.1 Переписать подраздел «Версия репозитория» в `README.md`: два носителя (`VERSION` и тег),
      таблица параметров запуска сокращается до одной строки `bump`, абзац про код задачи заменяется
      описанием исключения из GIT-009. Проверка: `grep -n "payload\.txt\|tools/version" README.md` находит совпадения
      только в строках таблицы версий — они исторические и по DOC-024 не переписываются
- [x] 5.2 В том же подразделе переписать перечень ограничений по
      [DOC-020](../../../rules/documentation-rules.md#полнота-содержания): пункт про отложенное
      обнаружение пропуска заменяется пунктом об отсутствии какого-либо напоминания о пропущенном
      релизе; пункт о выборе разряда сохраняется без упоминания задачи; пункт о принадлежности
      механизма репозиторию перечисляет оставшийся состав — `VERSION` и `release.yml`.
      Проверка: перечень ограничений не обещает несуществующей проверки
- [x] 5.3 Дописать строку в таблицу версий `README.md` по DOC-022 и DOC-024: версия `1.3.2`, задача —
      OpenSpec change `simplify-versioning`. Проверка: новая строка последняя, номер больше
      предыдущего

## 6. Приёмка

- [x] 6.1 Прогнать установку в предварительном просмотре в чистом временном каталоге и сверить
      состав файлов с манифестом до изменения. Проверка: перечни совпадают, установщик завершается
      кодом `0`
- [x] 6.2 Прогнать `sh tools/make-lint/check.sh`. Проверка: код возврата `0` — изменение не затронуло
      `Makefile`, но прогон подтверждает, что удаление `tools/version/` не задело соседнюю проверку
- [x] 6.3 Выпустить релиз: запустить сценарий `release` в GitHub Actions с разрядом `minor`.
      Проверка: в `main` появился коммит с заголовком `Релиз vX.Y.Z`, `VERSION` содержит новый номер,
      опубликован аннотированный тег `vX.Y.Z`, указывающий на этот коммит
