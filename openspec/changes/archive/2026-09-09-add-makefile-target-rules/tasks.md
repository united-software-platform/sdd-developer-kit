## 1. Документ правил

- [x] 1.1 Создать `rules/makefile-rules.md` по `DOC-014`: заголовок, назначение (1–3 строки),
  навигация; проверить, что уровни заголовков идут без пропусков (`DOC-012`)
- [x] 1.2 Заполнить таблицу правил `MAKE-001`..`MAKE-005` по составу из `design.md` — Решение 1;
  проверить, что каждый идентификатор из таблицы раскрыт отдельным разделом (`DOC-010`)
- [x] 1.3 Описать в разделах: определение секции (совпадает с определением проверки), норму 30 строк,
  способы устранения превышения, требование к под-целям после дробления, обязательный локальный прогон
  `sh tools/make-lint/check.sh`, храповик предела в пайплайне; проверить, что каждый способ устранения
  сопровождается примером команды в fenced code block с языком (`DOC-016`)
- [x] 1.4 Зафиксировать в документе действующее расхождение: блокирующий предел пайплайна 61,
  удерживающая секция `init-host`, целевое значение 30 (`DOC-020`); проверить, что названы и предел,
  и причина
- [x] 1.5 Добавить таблицу версий по `DOC-022`/`DOC-024` (версия `1.0.0`, дата, задача — OpenSpec
  change `add-makefile-target-rules`, агент, модель); проверить, что таблица — последний раздел файла

## 2. Подключение правил в контекст агента

- [x] 2.1 Добавить в `CLAUDE.md` `@rules/makefile-rules.md` в блок импортов раздела «Правила кода»
  и строку списка со ссылкой `[Makefile](./rules/makefile-rules.md)` с кратким описанием;
  проверить, что порядок и оформление совпадают с соседними строками
- [x] 2.2 Дописать строку в таблицу версий `CLAUDE.md` по `DOC-024` (в конец таблицы);
  проверить, что предыдущая последняя версия — `1.0.4`, а новая идёт следом
- [x] 2.3 Проверить, что все добавленные ссылки относительные (`DOC-015`) и разрешаются:
  `test -f rules/makefile-rules.md`

## 3. Проверка нормы в GitHub Actions

- [x] 3.1 Добавить в `.github/workflows/makefile-lint.yml` второй шаг «Норма 30 строк
  (информационно)» с `continue-on-error: true`, вызывающий `sh tools/make-lint/check.sh`
  без `--max-lines`; проверить, что блокирующий шаг остаётся первым и его предел не изменён
- [x] 3.2 Дополнить комментарий у блокирующего шага: назвать удерживающую секцию (`init-host`, 61)
  и целевое значение 30; проверить, что комментарий объясняет обе величины предела
- [x] 3.3 Проверить синтаксис workflow: `python3 -c "import yaml,sys;
  yaml.safe_load(open('.github/workflows/makefile-lint.yml'))"` завершается без ошибки

## 4. Проверка результата

- [x] 4.1 Локально воспроизвести оба прогона пайплайна: `sh tools/make-lint/check.sh --max-lines 61`
  завершается кодом 0, `sh tools/make-lint/check.sh` — кодом 1 и перечисляет `init-host` (61 строка
  при пределе 30)
- [x] 4.2 Убедиться, что `Makefile`, `tools/make-lint/check.sh` и состав поставки (`install.sh`)
  не изменены: `git status --porcelain` показывает только `rules/makefile-rules.md`, `CLAUDE.md`,
  `.github/workflows/makefile-lint.yml` и артефакты change'а
- [x] 4.3 Прогнать `openspec validate add-makefile-target-rules --strict` — изменений в артефактах
  после реализации быть не должно
