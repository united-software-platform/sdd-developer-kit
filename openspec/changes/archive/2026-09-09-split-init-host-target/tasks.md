## 1. Дробление секции init-host

- [x] 1.1 Вынести шаг файла секретов в цель `init-env` с описанием `##` и проверить, что
  `make init-env` в проекте без `.env` создаёт его из `.env.example`, а при существующем `.env`
  печатает прежнее сообщение и файл не меняет
- [x] 1.2 Вынести создание каталогов ключей и профилей в цель `init-dirs` и проверить, что
  `make init-dirs` создаёт `.ssh` и `.claude-accounts`, а права на `.ssh` равны `700`
  (`stat -c %a .ssh`)
- [x] 1.3 Вынести записи в `.gitignore` в цель `init-gitignore` и проверить, что `make init-gitignore`
  добавляет `.env`, `.ssh/`, `.claude-accounts/`, а повторный вызов ничего не дублирует
  (`sort .gitignore | uniq -d` пуст)
- [x] 1.4 Вынести проверку `ssh-keygen` и генерацию ключа проекта в цель `init-ssh-key` и проверить,
  что `make init-ssh-key` создаёт пару ключей с правами `600`/`644` и печатает публичный ключ,
  а повторный вызов оставляет ключ без изменений
- [x] 1.5 Вынести конфигурацию SSH для git-хоста в цель `init-ssh-config` и проверить три ветки:
  файл существует — не перезаписан; `GIT_HOST` пуст — сообщение о незаполненном хосте;
  `GIT_HOST` задан — файл создан с правами `644` и прежним содержимым
- [x] 1.6 Свести `init-host` к последовательным вызовам `@$(MAKE) --no-print-directory` пяти под-целей
  и проверить, что `make init-host` в чистом каталоге даёт тот же состав файлов, права и вывод,
  что и до дробления
- [x] 1.7 Дополнить `.PHONY` новыми целями и проверить, что цели выполняются при наличии одноимённых
  файлов в корне (`touch init-env && make init-env` отрабатывает шаг, а не сообщает `up to date`)
- [x] 1.8 Расширить ширину колонки имени в `help` до `%-16s` и проверить, что в выводе `make help`
  присутствуют все пять шаговых под-целей и колонка описаний не съезжает

## 2. Приведение проверки к норме

- [x] 2.1 Прогнать `sh tools/make-lint/check.sh` и убедиться, что вердикт — код возврата `0`
  и сообщение о пройденной проверке при пределе 30
- [x] 2.2 Снять ключ `--max-lines 61` с блокирующего шага `.github/workflows/makefile-lint.yml`,
  удалить информационный шаг «Норма 30 строк (информационно)» и переписать комментарии шага,
  описывающие долг; проверить синтаксис файла (`python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/makefile-lint.yml'))"`)
- [x] 2.3 Прогнать `sh tools/make-lint/check.sh --max-lines 61` и убедиться, что вердикт совпадает
  с прогоном по норме: долга, ради которого держался повышенный предел, больше нет

## 3. Синхронизация правил и документации

- [x] 3.1 Удалить из `rules/makefile-rules.md` раздел «Действующее расхождение» и его пункт навигации;
  проверить, что в файле не осталось упоминаний предела 61 (`grep -n "61" rules/makefile-rules.md` пуст)
- [x] 3.2 Дописать запись в таблицу версий `rules/makefile-rules.md` по [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа)
  и [DOC-024](../../../rules/documentation-rules.md#жизненный-цикл-документа): версия `1.0.1`,
  дата, задача — этот change, агент и модель, описание — удаление расхождения после дробления `init-host`
- [x] 3.3 Проверить, что упоминания предела 61 не остались нигде в репозитории вне архива change'ей
  (`grep -rn "max-lines 61" --exclude-dir=archive .` пуст)

## 4. Приёмка

- [x] 4.1 Выполнить `make help` и сверить перечень команд со сценарием «Под-цели видны в перечне команд»:
  присутствуют `init`, `init-host` и все её шаговые под-цели с описаниями
- [x] 4.2 Выполнить `make init-host` дважды подряд во временном каталоге с копией поставки и убедиться,
  что второй прогон не перезаписывает `.env`, ключ и конфигурацию SSH и сообщает, что они оставлены как есть
- [x] 4.3 Прогнать `openspec validate split-init-host-target --strict` и убедиться, что change валиден
